import { BaseSmser, SmsMessageContract } from '@ioc:Adonis/Addons/Sms'

export default class {{filename}} extends BaseSmser {
  /**
   * Uncomment the following line of code to use a different
   * smser and chain the ".options" method to pass custom
   * options to the send method
   */
  // public smser = this.sms.use()

  /**
   * The prepare method is invoked automatically when you run
   * "{{filename}}.send".
   *
   * Use this method to prepare the sms message. The method can
   * also be async.
   */
  public prepare(message: MessageContract) {
    message.from('+11111111111').to('+22222222222').text('This is the SMS message')
  }
}