/**
 * Config source:
 *
 * Feel free to let us know via PR, if you find something broken in this contract
 * file.
 */

import Env from '@ioc:Adonis/Core/Env'
import { smsConfig } from '@melchyore/adonis-sms/build/config'

export default smsConfig({
  /*
  |--------------------------------------------------------------------------
  | Default smser
  |--------------------------------------------------------------------------
  |
  | The following smser will be used to send sms, when you don't specify
  | a smser
  |
  */
  smser: '{{primaryDriver}}',

  /*
  |--------------------------------------------------------------------------
  | Default sender ID
  |--------------------------------------------------------------------------
  |
  | Global sender ID. Used by all drivers.
  |
  */
  from: 'AdonisJS',

  /*
  |--------------------------------------------------------------------------
  | Smsers
  |--------------------------------------------------------------------------
  |
  | You can define one or more smsers to send sms from your application. A
  | single `driver` can be used to define multiple smsers with different
  | config.
  |
  | For example: Twilio driver can be used to have different smsers for
  | sending transactional and promotional sms
  |
  */
  smsers: {
		{{#twilio}}
    /*
    |--------------------------------------------------------------------------
    | Twilio
    |--------------------------------------------------------------------------
    |
    | Uses Twilio for sending sms. You will have to install twilio
    | when using this driver.
    |
    */
    twilio: {
      driver: 'twilio',
      accountSid: Env.get('TWILIO_ACCOUNT_SID'),
      authToken: Env.get('TWILIO_AUTH_TOKEN'),
      // from: '+1111111111',
    },
		{{/twilio}}
		{{#vonage}}

    /*
    |--------------------------------------------------------------------------
    | Vonage
    |--------------------------------------------------------------------------
    |
    | Uses Vonage for sending sms. You will have to install @vonage/server-sdk
    | when using this driver.
    |
    */
    vonage: {
      driver: 'vonage',
      apiKey: Env.get('VONAGE_API_KEY'),
      apiSecret: Env.get('VONAGE_API_SECRET'),
    },
		{{/vonage}}
    {{#sns}}

    /*
    |--------------------------------------------------------------------------
    | SNS
    |--------------------------------------------------------------------------
    |
    | Uses Amazon SNS for sending sms. You will have to install aws-sdk
    | when using this driver.
    |
    */
    sns: {
      driver: 'sns',
      key: Env.get('SNS_ACCESS_KEY'),
      secret: Env.get('SNS_ACCESS_SECRET'),
      region: Env.get('SNS_REGION'),
      type: 'Promotional', // Or 'Transactional'
    },
		{{/sns}}
  },
})
